================================================================
Dual-PDH Controller — Firmware Assumptions & Notes
Rev 3.0 (2026-07-15) — v5 eurocard / universal I2C backplane
(supersedes Rev 2.0 pin map; lock architecture unchanged)
================================================================

ARCHITECTURE (Rev 2.0)
The kHz digital dither lock is replaced by a ~1-3 MHz ANALOG derivative lock.
The lock-in demod (AD831 mixer) and the PI servo are now analog HARDWARE.
The Teensy is a SUPERVISOR, not a real-time DSP loop. Per channel it:
  - programs a DRIVE DDS (modulates laser current at Omega) + a REFERENCE DDS
    (phase-set demod LO); both share the 25 MHz MCLK -> phase-coherent,
  - sweeps the DC current setpoint (DAC) to drag the laser across resonance,
  - on the discriminator zero-crossing, RELEASES the analog integrator via
    LOCKn_EN so the analog PI holds the lock,
  - calibrates the demod phase via the reference-DDS PHASE0 register,
  - monitors the post-mix baseband error on LOCKn_IN.
See docs/firmware_reference.html (Rev 2.0) and
docs/laser_driver_v3_rf_modulation_design.html for the full hardware design.

----------------------------------------------------------------
ASSUMED — verify / tune on the real system
----------------------------------------------------------------
ADC midscale: adc_to_error() assumes the baseband mixer error is centered at
  ADC_MID. If the front-end offset differs, change adc_to_error().

Modulation Omega: RF_OMEGA1_HZ / RF_OMEGA2_HZ default to 2.0 / 2.5 MHz. Set
  each just ABOVE the measured DFB thermal/carrier FM crossover (open item 1),
  and keep |Omega1 - Omega2| large vs the loop/LPF bandwidth. Live: omega1/2.

Demod phase: RF_PHASE1/2_DEG default 0. The true value depends on the detection
  path delay — run cal1/cal2 (park the laser so there IS a non-zero error first,
  e.g. on a resonance shoulder / 'hold'). VERIFY LOCK POLARITY: if the servo runs
  away, add 180 deg (the analog PI is inverting; phase + integrator-switch
  polarity must give negative feedback).

Capture/loss thresholds: lock_threshold / acquire_threshold are starting values
  (0.02 / 0.10). Tune to the mixer amplitude and noise floor. Live: thresh1/2.

Sweep rate: SWEEP_STEP_CODE (DAC code/tick at 1 kHz) sets the SEARCH speed.
  Too fast -> the discriminator zero-crossing is missed; too slow -> long
  acquisition. Tune to the discriminator width.

LOCKn_EN polarity: firmware convention is HIGH = RUN (loop closed),
  LOW = HOLD/RESET. Map this to the chosen analog switch (ADG419 etc.) in the
  PI_LoopFilter block. If inverted, flip set_integrator().

Integrator centering (OPTIONAL): if the CORR taps (PIN_LAS1/2_CORR_MON, A14/A16)
  are fitted and #defined in config.h, center_step() slowly trims the DC setpoint
  to off-load the analog integrator. The nudge SIGN (corr >> 6) depends on wiring
  -- confirm it drives toward centre, not the rail. Default: disabled (DC setpoint
  fixed after acquisition).

PID is LEGACY: pid[] no longer drives the lock (the PI is analog). p1/p2 set its
  gains only for the optional centering loop and the CLI/JSON readout.

CDCE913: programs EEPROM only when the version byte differs; force with 'r'.
  IMPORTANT (Rev 2.0): the 25 MHz must now fan out to FOUR AD9833 MCLK inputs.

----------------------------------------------------------------
PIN MAP (Rev 3.0) — v5 DIG eurocard, universal DIN 41612 backplane
----------------------------------------------------------------
Matches Lasser_Driver_v5/DIG_Card/DIG_Card.kicad_sch and config.h.
  D14/D15/D16      SPARE (was A0/A1/A2 monitors; the planned GA slot-strap
                   readback was dropped 2026-07-16 — cards self-address
                   with on-card jumpers, DIG enumerates by I2C probe)
  D31  PIN_FAULT_N shared open-drain fault, active-low (was HBRIDGE_FAULT1)
  A10/A11 LOCKn_IN  post-mix baseband error (ERR1/2_BUS) — UNCHANGED
  D2/D3/D6/D8 DDS chip-selects, D4/D5 DDS resets     — UNCHANGED
  D18/D19 I2C0 = SYSTEM BUS (CDCE913 on-card + all card peripherals)
  D26-D30 SPI1 TFT, D9 FreqCount                      — UNCHANGED
REMOVED (functions moved onto the CH/AFE cards over I2C):
  AD5064 (D10 CS, D7 CLR)      -> AD5696R per CH card (DAC A=laser, B=TEC)
  LOCKn_EN GPIOs (D33/D34)     -> PCA9538 IO0 per CH card
  HBRIDGE_FAULT1/2 (D31/D32)   -> PCA9538 IO1 + shared FAULT_n (D31)
  all analog monitors (A0-A3, A6-A9) -> ADS1115 per card (volts)
I2C addresses (from CH-card address jumpers; datasheet-verified bases —
AD5696R = 00011|A1|A0, PCA9538 = 0x70|A1<<1|A0. Hardware convention fixed
by the ADG419 mux truth table, IN=0 -> S1 = ERR1/MOD1: ch1 = GA0=0 =
JP101 FITTED; ch2 = GA0=1 = JP101 OPEN. See manual section 4.2):
  CDCE913 0x65 | CH1: ADS1115 0x48, AD5696R 0x0C, PCA9538 0x70
              | CH2: ADS1115 0x49, AD5696R 0x0D, PCA9538 0x71
              | AFE: ADS1115 0x4A (ADDR->SDA strapped on-card)
Noise rules: telemetry <=10 Hz round robin, suspended during ACQUIRE;
DAC/expander writes are cached (no I2C traffic when values unchanged);
the only bus pullups are R112/R113 (4.7k) on the DIG Clock section.

//////////////////////////////////////////
//  Runtime serial commands (USB / Telnet / Web)
//////////////////////////////////////////

s / status        — channel states, RMS, temps, currents, Omega/phase
lock1 / lock2      — start sweep-to-lock (analog PI takes over on capture)
stop1 / stop2      — to IDLE
hold1 / hold2      — freeze (integrator held)
break1 / break2    — break lock -> RELOCK
reset1 / reset2    — clear relock counter

omega1 / omega2 <hz>   — set CH modulation Omega (100k-3.5M); reprograms
                          drive+reference pair phase-coherently
phase1 / phase2 <deg>  — set demod phase live (reference DDS PHASE0)
cal1 / cal2            — auto-calibrate demod phase (park on a shoulder first)
thresh1 / thresh2 <lock> <acq> — capture/loss thresholds

p1 / p2 kp ki kd  — LEGACY PID gains (servo is analog; centering loop only)
dither <hz>       — LEGACY: set BOTH channels to the same Omega
d                 — dump sensors (LOCKn in ADC counts; monitors in VOLTS via I2C)
r                 — force CDCE913 EEPROM reprogram
help              — full command list
(demo on/off, netinfo, netset, netreset — unchanged)

----------------------------------------------------------------
CHANGELOG
----------------------------------------------------------------
Rev 3.3 (2026-07-17) — MPD optical-power (NTC_A offset removal)
  - CH card MPD_MON = NTC_A − I_pd·R_bias1 (PD cathode node buffered by
    U_TIA1; R_bias1 20k ties it to U_TIA2 virtual gnd @ NTC_A). Added
    mpd_to_optical_mw(mpd_v, ntc_v) = (NTC_A − MPD_MON)·MPD_MW_PER_V,
    clamp >=0 (chcard.h). NOTE sign: NTC_A − MPD_MON (PD pulls MPD_MON
    below NTC_A), NOT MPD_MON − NTC_A.
  - config.h += MPD_R_BIAS_OHM 20k, MPD_RESP_UA_PER_MW 2.2 (DFB1550LP),
    MPD_MW_PER_V 22.7. chcard.h now #includes config.h + math.h.
  - 'd' telemetry prints POPTn (mW) beside raw MPDn. Verify vs a
    calibrated power meter at bring-up. Recompile (needs rebuild).

Rev 3.2 (2026-07-17) — INA828 swap + IMON cal constants
  - CH card U28/U32 changed INA826 -> INA828IDR (826 EOL-ish at DigiKey;
    828 is the pin-compatible successor, G = 1 + 50k/RG).
  - TEC_IMON_V_PER_A 0.72 now EXACT (G = 1+50k/8.06k = 7.20).
  - LASER_IMON_MA_PER_V corrected 100 -> 38.6 (INA828 G=103.7 x 0.25R
    shunt = 25.93 V/A; the old 100 was a v3 placeholder that never
    matched the INA chain). Verify both against measured currents at
    bring-up. Requires recompile (constants only).

Rev 3.1 (2026-07-16) — jumper-based card addressing
  - Card address moved from backplane slot straps to ON-CARD jumpers
    (CH card rev B: JP101-103, fitted = 0). CH1/CH2 I2C address
    constants SWAPPED in config.h to match the hardware convention
    (ADG419 IN=0 -> S1 = ERR1/MOD1 => ch1 = GA0=0 = JP101 fitted):
    CH1 now 0x48/0x0C/0x70, CH2 0x49/0x0D/0x71. AD5696R/PCA9538
    address bases verified against datasheets (VERIFY note cleared).
  - PIN_GA0-2 defines and the setup() slot-GA readback REMOVED;
    D14-16 spare. Card presence = I2C probe of the known address set.
  - Compiled successfully under Teensyduino 2026-07-16 (no errors);
    not yet run on hardware.

Rev 3.0 (2026-07-15) — v5 eurocard / universal I2C backplane
  - config.h: pin map reworked (GA0-2 on D14-16, FAULT_n on D31; AD5064,
    LOCKn_EN GPIOs, HBRIDGE_FAULTn and all analog monitor pins removed);
    +I2C address map; +TELEM_PERIOD_MS; LASER_IMON_MA_PER_LSB ->
    LASER_IMON_MA_PER_V, +NTC_VSUP (monitors are volts now).
  - NEW ad5696.*, ads1115.*, pca9538.*, chcard.* (per-card abstraction:
    cached DAC/LOCK_EN writes, non-blocking ADS1115 round robin, fault query).
  - DELETED ad5064.* .
  - .ino: monitors moved from 1 kHz analogRead to 10 Hz I2C telemetry
    (suspended during ACQUIRE); FAULT_n edge-triggered source query;
    setup() reads slot GA, reports CH-card I2C presence; conversions
    now volts-based (volts_to_tec_amps, ntc_to_celsius(v)).
  - comms.cpp: 'd' prints I2C telemetry in volts + AFE PD levels;
    demo-off safe state via chcard_set_midscale_all() (TEC parks at
    the 0 A code, safer than the old midscale).
  - NOT compile-verified in this pass (no Teensy toolchain on this
    machine) — build under Arduino/Teensyduino before flashing.

Rev 2.0 (2026-06-04) — RF analog-derivative lock
  - config.h: +PIN_AD9833_3/4_CS (D6/D8), +PIN_LOCK1/2_EN (D33/D34),
    optional CORR taps (A14/A16); +RF_OMEGA1/2_HZ, RF_PHASE1/2_DEG, sweep/cal
    constants; LOCKn_IN re-documented as baseband error.
  - ad9833.*: +phase register (PHASE0); +ad9833_load()/ad9833_run() split for
    phase-coherent pair start; +ad9833_set_phase() live; +ad9833_reset_sw().
  - .ino: control_step() reworked to sweep-acquire + integrator hold/run (no
    digital servo); +set_integrator(), rf_set_omega(), rf_set_phase(),
    rf_phase_cal(), center_step(); setup() brings up 4 DDS phase-coherent.
  - comms.cpp: +omega1/2, phase1/2, cal1/2; dither -> legacy (both Omega);
    p1/p2 -> legacy; status shows Omega/phase; help updated.
  - PID retained for CLI/JSON + optional centering only.
  - Compiles clean under Arduino/Teensyduino (verified).

Rev 1.0 (2026-05-30) — kHz digital dither lock with Teensy digital lock-in/PID.
